"""Tool modules for the notebooklm-mcp server.

Each submodule registers its tools against the shared ``FastMCP`` instance
from :mod:`_runtime`. ``server.py`` simply imports each submodule to trigger
decoration.
"""
